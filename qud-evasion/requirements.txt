# Pin exact versions on the GPU server with `pip freeze > requirements.lock`
# before the final runs, and commit the lock file for reproducibility.
datasets>=2.19
pandas>=2.1
pyarrow>=15.0
numpy>=1.26
scikit-learn>=1.4
transformers>=4.44
torch>=2.3
accelerate>=0.30
sentencepiece
vllm>=0.5          # offline LLM inference; requires CUDA
sentence-transformers>=3.0
joblib
tqdm
pyyaml
